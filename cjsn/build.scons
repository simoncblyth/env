##
Import("env")
ec = env.Clone()

cjsn = ec.ComponentLibrary("cjsn",["src/cJSON.c"])

ec.Append( LIBS=cjsn )
ec.ComponentTestProgram( 'test_cjsn', ['src/test.c'] )

EIncludes( ec, ['src/cJSON.h'] )

